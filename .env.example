# PostgreSQL
DATABASE_URL=postgresql+asyncpg://carepulse:carepulse@postgres:5432/carepulse
POSTGRES_USER=carepulse
POSTGRES_PASSWORD=carepulse
POSTGRES_DB=carepulse

# Auth
JWT_SECRET=change-me-in-development
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (comma-separated). Do not use * in production-shaped config.
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Demo seed credentials (synthetic prototype only)
ADMIN_DEMO_PASSWORD=CarePulseAdmin!23
CLINICIAN_DEMO_PASSWORD=CarePulseClinician!23

# ML
MODEL_PATH=/app/models/rf_v1.joblib
FEATURE_VERSION=fv1
